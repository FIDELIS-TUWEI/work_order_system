import { Box, styled } from "@mui/material"

const Header = () => {

  const StyleHeader = styled(Box)({
    padding: "10px",
    backgroundColor: "red",
    minHeight: "400px"
  })

  return (
    <>
      <StyleHeader>

      </StyleHeader>
    </>
  )
}

export default Header